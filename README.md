# ts-graylog

TypeScript Graylog client library for Node.js, based on [node-graylog2](http://github.com/Wizcorp/node-graylog2). This has been heavily modified to the point where there is not much left of the original.

It is a rewrite using TypeScript.
